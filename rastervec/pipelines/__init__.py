"""Pipeline packages: readable, flat pipeline definitions.

Each module under here is a short block sequence of high-level calls --
read these to understand what the system actually runs. New capability =
a new named call added to one of these files.
"""
